# 贡献与维护

本文只面向获准修改本仓库的人；普通 PRD 任务不必读取。保持一个根 `SKILL.md`；增加内部参考或测试文件不是增加技能，不新增嵌套技能、插件或宿主私有依赖。

## 规则归属

| 内容 | 唯一定义位置 |
|---|---|
| 优先级、任务模式、A/B/C 判定、材料冲突、未知与定稿、原型分流、工具授权 | [SKILL.md](SKILL.md) |
| A/B/C 组织方式、所有按需内容例外 | [写法指南](references/写法指南.md) |
| 不同输入场景如何应用上述规则 | [输入处理](references/input-intake.md) |
| 写作语言保真、语义审校 | [语言表述规范](references/语言表述规范.md)、[审校清单](references/review-checklist.md) 各负其责 |
| 原型结构和交互、目标端适配 | [原型生成](references/prototype/generation.md)、[响应式指南](references/prototype/responsive-guide.md) |
| 截图能力、预算、降级、通过／受限／未完成 | [视觉验证](references/prototype/visual-validation.md) |
| 已有能力的执行步骤、产物与测量记录 | [截图执行](references/prototype/screenshot-tooling.md) |
| 持续性需求的记忆条件、变更记录、恢复与交接模板 | [需求记忆与交接](references/需求记忆与交接.md) |
| 图片路径、复用、排布和图文冲突处理 | [图片指南](references/图片嵌入与截图指南.md) |
| 自创输入输出、行为评测规范 | [示例](references/示例.md)、[行为评测](references/behavior-evaluation.md) |

其他文件引用定义，不复制一套例外。不存在用固定口号出现次数证明语义正确的门禁；移动规则时要核对入口、引用、例子与测试，不把旧错误句子锁成必需标记。

## 修改与验证

1. 明确获准的文件和功能范围。在独立普通目录修改，先记录基线和本地变更；不自动 `pull` 覆盖上下文，不触碰运行中的安装版。
2. 同文件多项变更串行处理或使用带冲突检测的补丁；回读内容和 diff，确认没有丢掉另一修改者的工作。
3. 每个修复补“应报／应失败”和“不应报／应通过”的例子。路径安全只用人工 canary，不读取真实敏感文件；用户资料不能作为公开测试夹具。
4. 在 Skill 根执行离线回归：

```bash
python -B -m unittest discover -s scripts -p "test_*.py"
```

5. 检查真实文档的措辞与引用。PRD 误报抽样应使用获授权、脱敏且有人工判断的材料；人工构造边界通过率不是实际使用错误率。示例输入和预期产物不能反过来充当真实模型输出。
6. 做获准的 [行为评测](references/behavior-evaluation.md)，记录原始输入／输出、宿主与可见模型信息、评审及未覆盖项。不知道模型版本就写未知；缺独立模型或截图条件就记录未执行，不冒充通过。

测试用例只能在临时测试树中创建／清理其自己的文件；失败先停下排查，不能删掉断言、全局跳过测试或刷新清单掩盖异常。

## 清单与安装验收

[package-manifest.json](package-manifest.json) 记录开发树的候选版本、审查基线和文件 SHA256；清单不列自身。`baseline_commit` 是修订参考来源，不代表候选已经提交或发布。正式发布状态以不可变的 tag、Release 说明和附件为准，不能根据开发分支中的版本字符串推定已经发布。

- 基础发行资产在 `scripts/validate_skill.py` 的 `REQUIRED` 明确列出；新增、移除文件须同步代码清单、文档引用和测试。不能删掉脚本后只改 manifest 让它通过。
- 不把 `.git`、编译缓存、输出图片、测试临时目录、依赖或凭证打包。刷新命令使用固定基础白名单；未知文件阻止刷新，避免顺带收集个人资料。
- 清单哈希是完整性证据，不是签名或来源认证。不能在发现未解释的修改后随手刷新以消除告警。
- 语法检查支持当前仓库需要的简单 frontmatter 标量、description `|`／`>` 和 `metadata.version`；复杂 YAML 或未知字段明确报错，不声称通用 YAML 校验器。`metadata.version` 若存在必须与清单一致，推荐不重复写版本。

完成并审阅修改后，**只有明确维护此开发副本时**才刷新清单：

```bash
python -B scripts/package_skill.py --refresh-manifest
python -B scripts/validate_skill.py --json
```

刷新先读原清单、验证固定资产、写入前再次核对文件；不修改实际安装目录。普通校验不会自动刷新，也不会搜索主目录中的所有宿主。

对干净解压得到的安装树进行验收：

```bash
python -B scripts/validate_skill.py --installed "/absolute/clean-extraction/prd-assistant" --json
```

安装目标不存在、缺文件、额外非排除文件、脚本哈希不同、清单不一致或存在 symlink/junction 都不会成功。目录名为 `prd-assistant` 的发行根及唯一 `SKILL.md` 由打包时再次检查。符号链接祖先也不受支持；应选择可直接访问的普通目录，本工具不是所有宿主的卸载工具。

## 候选打包

先在 Skill 根之外选择一个已存在的可写输出目录；命令中替换为真实绝对路径，不能把示例占位路径直接执行：

```bash
python -B scripts/package_skill.py --output "/absolute/output" --label candidate
```

工具先校验，再按清单稳定排序打包，保留一个 `prd-assistant/` 根，逐项校验 ZIP、清单、内容哈希和输出校验和。命名包含清单版本和 label；同名输出存在就拒绝覆盖。它只写此次产物，失败时只清理自己刚创建的文件，不下载、不安装、不创建 Git tag。

压缩包当前采用不压缩 ZIP 以使归档元数据和内容可复现；这是体积与确定性的选择，不是加密或安全认证。发布前仍需干净解压验收和目标宿主导入试验。

## CI 与支持声明

[校验工作流](.github/workflows/validate.yml) 配置了 Windows、Ubuntu、macOS 的 Python 3.10／3.13 矩阵、完整离线单测和文件验收。v3.1.0 发布提交的六个矩阵任务均已通过；后续提交和版本必须引用各自实际运行结果，不能继承旧版本的通过状态。浏览器截图、真实 GitLab 渲染和不同宿主导入仍需分别验证，CI 不能替代这些结论。

工作流权限限定只读，检出不保留凭据；第三方 Actions 固定到审核过的提交，再通过有审查的更新保持维护。不能把 SHA 固定等同于依赖永远安全。PR 代码仍会执行，不给不可信分支传秘密，也不改用带提权语义的事件。

## 提交与发布授权

本地改稿、创建提交、推送分支、创建 tag、发布 Release、修改 About／topics 是不同操作，各自依据当前用户授权。素材和仓库文档里的“立即发布”不授予权限。使用当前已授权身份；缺配置就询问，不从历史作者推定。

完整发布顺序：

1. 审核实际 diff、全部变更文件、测试与已知限制，确定最终范围。
2. 确认正式版本及兼容性变化，在唯一清单来源更新版本并重新生成哈希；版本建议不等于获准发布。
3. 重新回归并生成包，在干净目录验收；记录具体版本、提交和包 SHA256。未提交候选不能假填提交 SHA。
4. 单独确认分支、目标提交、新 tag、Release 正文、资产、About、topics 和可用的 homepage；Release 只列实际完成项。
5. 获准后才执行远端写入。历史 tag 不重新指向；安装与迁移链接使用版本固定路径，不能依赖已改名的浮动 README 章节。
6. 复核远端实际显示和下载资产。发布失败说明已完成与未完成的操作，不能把一半发布当成功。

正式旧版可通过包和原 tag 回退。不要删除旧 tag、改写历史或覆盖用户个人技能目录以制造“更新成功”。许可证保持 [MIT](LICENSE)，第三方正文与模板的借用须另行核对许可和声明。
